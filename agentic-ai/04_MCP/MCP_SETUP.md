# Configuration et Vérification du Serveur MCP

## Informations du Serveur
- **Nom du serveur MCP** : `crm-server`
- **Nom du Tool CRM détecté** : `get_customer_status`

## Résultat de la vérification
Le serveur MCP d'entreprise (`crm-server`) a été correctement chargé par VS Code via la configuration `.vscode/mcp.json`. 

Lors de l'interrogation de Copilot Chat en mode Agent avec la question *"Quels sont les outils externes (MCP) auxquels tu as accès ?"*, l'assistant a confirmé la présence et la disponibilité de l'outil `get_customer_status`.